org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=false

android.useAndroidX=true
android.nonTransitiveRClass=true

kotlin.code.style=official

# Set to false to point the app at a real REST backend instead of the
# bundled asset-backed mock server. See README -> "Switching to a live API".
instantmechanic.useMockApi=true
instantmechanic.baseUrl=https://api.instantmechanic.dev/v1/
